
            Create detailed React components with these requirements:

1. Use functional components with arrow syntax and proper TypeScript types
2. Style with styled-components or CSS Modules for maintainable CSS
3. Use @heroicons/react for icons (from @heroicons/react package). Do NOT use other UI libraries unless requested
4. Use stock photos from unsplash.com where appropriate, only valid URLs you know exist
5. Configure webpack/vite image loaders to enable stock photos from unsplash.com
6. Avoid duplicate components and implement proper code splitting
7. Follow component folder structure: /components/{common,features,layouts}
8. Follow proper import practices:
    - Use absolute imports with path aliases
    - Keep component imports organized
    - Update component files with comprehensive code
    - Don't forget proper TypeScript types
    - You MUST complete the entire component before stopping

   <Image-Analysis>
1.  **Layout Structure:**
    *   **Overall layout organization:** The UI employs a left-aligned sidebar navigation with a main content area to its right. This is a common pattern for desktop applications and web dashboards, providing persistent navigation while maximizing space for content. The main content area uses a loose grid system, primarily organizing sections and content cards in horizontal rows.
    *   **Spacing and alignment patterns:**
        *   **Vertical Spacing:** Consistent vertical spacing is used between major sections (e.g., "继续阅读", "之前读过", "阅读目标"). Within sections, elements like card titles and subtitles have smaller, yet consistent, vertical gaps.
        *   **Horizontal Spacing:** There's clear horizontal padding separating the sidebar from the main content. Within content rows, items (like book cards) maintain a consistent horizontal gap (approximately 16-24px). Text within cards is left-aligned.
        *   **Alignment:** Headings are left-aligned. Content cards within their respective sections are generally left-aligned or centrally aligned within their allocated columnar space. The navigation items in the sidebar are also left-aligned. The top-left window controls (red, yellow, green circles) suggest a macOS-like interface.
    *   **Responsive design considerations:** Given the fixed sidebar width and the horizontal arrangement of content cards, this design is primarily optimized for wider screens (desktop/laptop). For smaller screens, the sidebar would likely collapse into a hamburger menu or similar mobile-friendly navigation. The content cards would likely stack vertically rather than horizontally.
    *   **Content hierarchy and visual flow:**
        *   The left sidebar holds primary navigation, always visible, establishing user location and available actions.
        *   The main content area begins with a prominent "主页" title, clearly indicating the current view.
        *   Sections like "继续阅读" (Continue Reading) and "之前读过" (Previously Read) are immediately below, using larger headings and more prominent cards to draw attention to ongoing and recent activities—a clear priority in user flow.
        *   The "阅读目标" (Reading Goal) section is further down, using a smaller heading but a larger, more engaging visual (circular progress bar) to encourage interaction.
        *   Text within cards follows a hierarchical flow as well: title > author/publisher > progress/status.

2.  **Components:**
    *   **List and describe all UI components:**
        *   **App Bar/Title Bar:** Top-left corner with standard window controls (red, yellow, green circles).
        *   **Sidebar:** Left-hand vertical panel for navigation. Background color (approx. #F5F5F5).
        *   **Navigation Items (Sidebar):**
            *   **Icon-only items:** Top group of small, square buttons/icons (e.g., mail, bell, settings) with associated numerical badges (e.g., "0" with "5").
            *   **Text-with-icon items:** "主页" (Home), "全部" (All), "欲读清单" (Wishlist), "已读完" (Finished Reading), "新建藏书" (New Collection). These are list items within categories.
            *   **User Profile Card:** Bottom-left of sidebar, displaying a circular avatar and user name ("Ralap Chi").
        *   **Main Content Area:** The larger right-hand panel where main content is displayed. Background color (approx. #FFFFFF).
        *   **Section Headers:** Large text labels for content blocks (e.g., "主页", "继续阅读", "之前读过", "阅读目标"). Some include an arrow icon for expansion/navigation ("之前读过 >").
        *   **Book Cards (Horizontal - "继续阅读"):** Rectangular cards with an embedded book cover image on the left, book title (large font), author, and reading progress (e.g., "图书 • 11%") stacked vertically to the right. Rounded corners. Dark background.
        *   **Book Cards (Vertical - "之前读过"):** Smaller rectangular cards with a book cover image at the top-left, followed by the book title (medium font), author, and status (e.g., "已读完" with a checkmark, or "图书 • 1%") below. Some also show a cloud icon (download/sync status). Rounded corners. Lighter background.
        *   **Descriptive Text:** Sub-text under section headers (e.g., "坚持每天阅读，提升你的数据，以激励你读完更多图书。").
        *   **Progress Indicator (Circular):** Large circular shape representing a reading goal/progress, with a central text display ("今日阅读进度" and "0:00"). Light grey circle with white inner area.
        *   **Scrollbar:** A thin, light grey vertical scrollbar on the far right of the main content area, indicating more content below.
    *   **Component hierarchy and relationships:**
        *   The main view is composed of a `Sidebar` and a `Main Content Area`.
        *   The `Sidebar` contains a `Navigation Group` (top icons), `Navigation Categories` ("书库", "我的藏书") which hold `Navigation Items`, and a `User Profile Widget`.
        *   The `Main Content Area` is divided into `Sections`, each with a `Section Header` and a collection of `Content Cards` (e.g., `Book Cards`) or a `Progress Widget`.
    *   **Interactive elements:**
        *   **Navigation Items:** "主页", "全部", "欲读清单", "已读完", "新建藏书" are clickable. "主页" is currently selected. The small icons at the top of the sidebar are likely interactive.
        *   **Book Cards:** Likely clickable to open the book or its details page.
        *   **"之前读过 >" Arrow:** Indicates a clickable element to view more past reads.
        *   **"新建藏书" Button:** A plus icon with text, implying an action to create a new collection.
    *   **Navigation elements:**
        *   **Sidebar Navigation:** Primary global navigation, providing access to different views like Home, All books, Wishlist, Finished books, and custom collections.
        *   **Breadcrumbs/Contextual Navigation:** Not explicitly present in this specific view, but "主页" functions as the current page indicator.
    *   **Special features or widgets:**
        *   **User Profile Widget:** At the bottom of the sidebar, displaying an avatar and name.
        *   **Reading Goal/Progress Widget:** The large circular progress indicator with time tracking functionality, designed to engage users with their reading habits.

3.  **Visual Design:**
    *   **Color palette:**
        *   **Primary Background:** White (#FFFFFF) for the main content area.
        *   **Sidebar Background:** Light Grey (#F5F5F5 / rgba(245, 245, 245, 1)).
        *   **Selected Navigation Item:** Light Purple/Lavender (#E8E7ED / rgba(232, 231, 237, 1)) or similar, with a slightly darker text/icon color for contrast.
        *   **Text Colors:**
            *   Dark/Black for headings and primary text: Likely close to #333333 or #000000.
            *   Grey for secondary text/labels: Around #666666 or #999999 (e.g., author names, progress percentages, descriptive text).
        *   **Icon Colors:** Varied, but often dark grey or subtle pastels. The top-right badge is light blue (#BBE6F5) with a light blue number. Notification icons on the top left of the sidebar are also light grey/white on light backgrounds.
        *   **Book Card Backgrounds:**
            *   "继续阅读" (Dark): Dark Green-Grey (#4D5857 / rgba(77, 88, 87, 1)) and Dark Red-Grey (#4D3D40 / rgba(77, 61, 64, 1)).
            *   "之前读过" (Light): Off-white/Light Grey (#FFFFFF with a subtle shadow - see below).
        *   **Accent Color:** A subtle blue-green seems to be used for the active selection circle in the "已读完" status and possibly the top-right badge, but exact hex is unclear without inspection.
    *   **Typography:**
        *   **Font Family:** Appears to be a clean, modern sans-serif typeface, possibly a system font (like PingFang SC for Chinese or a common sans-serif for general use) or a custom font. Its clean lines and readability are suitable for digital content.
        *   **Sizes (approximate):**
            *   "主页" Heading: Large, approx. 28-32px.
            *   Section Headers ("继续阅读", "之前读过", "阅读目标"): Medium-large, approx. 18-22px.
            *   Book Titles (large cards): Medium, approx. 16-18px.
            *   Book Titles (small cards): Medium-small, approx. 14-16px.
            *   Author/Progress Text: Small, approx. 12-14px.
            *   Sidebar Navigation Text: Medium-small, approx. 14-16px.
            *   "今日阅读进度" / "0:00": Large, "0:00" particularly prominent, approx. 40-50px.
        *   **Weights:**
            *   Headings: Bold or Semi-bold.
            *   Book Titles: Semi-bold.
            *   Author/Progress: Regular.
            *   Sidebar Text: Regular for unselected, Semi-bold for selected.
            *   "0:00": Bold.
    *   **Shadows and elevation:**
        *   Subtle shadows are used to give elements a sense of depth and separation from the background.
        *   **Card Shadows:** Book cards in both sections have very soft, diffused shadows, suggesting a slight lift off the background. The shadow is barely visible but present, likely `box-shadow: 0px 2px 8px rgba(0, 0, 0, 0.1)`.
        *   **Sidebar Shadow:** A very faint vertical shadow might exist on the right edge of the sidebar, or it's simply the contrast between the darker sidebar and white main content.
        *   **Top-level Container Shadow:** The entire application window seems to have a subtle shadow, giving it an elevated, desktop-app feel.
    *   **Border styles and corner radiuses:**
        *   **Corner Radiuses:** Uniformly rounded corners are present on almost all interactive containers:
            *   Book cards (both types): Approximately 8-12px radius.
            *   User profile avatar: Fully rounded (circular).
            *   Top-left window controls: Fully rounded buttons.
            *   Sidebar top corners: Rounded, matching the overall app window.
            *   Main content area top-right corner: Rounded, matching the overall app window.
            *   Progress indicator circle: Perfectly round.
        *   **Border Styles:** Generally borderless for cards, relying on shadows and background colors for definition. Some icons or UI elements might use thin borders (e.g., the "新建藏书" plus icon might have a subtle circle border if active/hovered).
    *   **Icons and visual assets:**
        *   **Icons:**
            *   System Icons: Standard macOS-style window controls (red, yellow, green circles).
            *   Navigation Icons: Line-art style icons (e.g., home, folder, arrow, checkmark, plus sign) in the sidebar. Used for clarity and quick recognition.
            *   Status Icons: Small icons like the checkmark for "已读完" and the cloud for download/sync.
            *   Profile Avatar: A small, circular image representing the user.
        *   **Book Cover Images:** Miniatures of book covers are consistently used within the book cards as primary visual identifiers for each book.

4.  **Micro-interactions & States:**
    *   **Hover states:** Not visually depicted, but it's highly probable that:
        *   Navigation items in the sidebar would change background color or text color on hover.
        *   Book cards would show a subtle lift (increased shadow) or a slight background change on hover.
        *   The "之前读过 >" arrow might change color or become more prominent.
    *   **Active/Selected states:**
        *   **Sidebar Navigation:** "主页" is clearly in an active state, indicated by a different background color (light purple/lavender) and possibly a bolder text/icon color. This highlights the current page.
        *   **"已读完" (Finished Reading) Status:** The checkmark icon indicates a completed state.
    *   **Transitions and animations:** Not visible in a static image, but smooth transitions would be expected for:
        *   Navigation item selection (e.g., fade or slide of background).
        *   Card hovers (e.g., subtle shadow expansion).
        *   Progress bar updates (e.g., smooth animation of the circle filling up).
    *   **Loading states:** Not shown, but would typically involve:
        *   Spinners for sections loading content.
        *   Skeleton screens (placeholder shapes) for cards while data is fetched.
    *   **Error states:** Not shown, but expected to:
        *   Display clear error messages with appropriate iconography (e.g., warning triangle, X mark).
        *   Highlight input fields with red borders if validation fails.

5.  **Additional Design Details:**
    *   **Accessibility considerations:**
        *   **Color Contrast:** Text on cards and backgrounds generally has good contrast, especially the dark text on light backgrounds. However, the dark text on dark green/red book cards could be challenging for some users without high contrast.
        *   **Typography:** The font is clean and legible, aiding readability.
        *   **Interactive Elements:** Elements like buttons and cards appear to have sufficient size for touch/click targets.
        *   **Semantic Structure:** The clear headings and sectioning contribute to a more accessible structure, which screen readers can interpret.
    *   **Consistency patterns:**
        *   **Corner Radius:** Consistently applied rounded corners across almost all rectangular UI elements, creating a soft and modern aesthetic.
        *   **Spacing:** Regular vertical padding between sections and horizontal spacing between cards.
        *   **Iconography:** Uses a consistent line-art style for navigation and status icons.
        *   **Card Structure:** Although two types of book cards exist, their internal layout (image, title, author, status) follows a consistent pattern within their respective groups.
    *   **Unique design elements:**
        *   **Two-tiered Book Cards:** The "继续阅读" cards are visually distinct (darker, more prominent) compared to the "之前读过" cards (lighter, more compact), effectively conveying a difference in priority or status.
        *   **Reading Goal Widget:** The large, central circular progress indicator is a key visual feature for encouraging engagement and tracking progress, making it stand out.
        *   **Subtle Background Gradient/Shift:** The very subtle color change from the sidebar (#F5F5F5) to the main content (#FFFFFF) provides a clean separation without harsh lines.
    *   **Areas for potential improvement:**
        *   **Scrollability in "之前读过":** The horizontal scroll might not be immediately obvious if the scrollbar is hidden or very thin. An explicit scroll indicator or navigation arrows could improve discoverability, especially if there are many books in this category.
        *   **Consistency of Book Card Information:** While "继续阅读" shows "图书 • 11%", "之前读过" shows "已读完" or "图书 • 1%". If there's a standardized way to show progress or completion, it could be applied more consistently across card types (e.g., always showing a percentage, even if 100%).
        *   **Visual Cues for Draggable/Rearrangeable Items:** If the books in "我的藏书" are meant to be draggable or reorderable, a subtle visual cue (e.g., a handle icon) would be helpful.
        *   **Tooltip/Labeling for Top Sidebar Icons:** The top-left sidebar icons are very small and currently lack text labels. Tooltips on hover would be crucial for discoverability and accessibility.
        *   **Empty States:** No empty states are shown (e.g., what if there are no books in "继续阅读" or "之前读过"?). Clear empty state designs are important for user experience.
        *   **Number Badge on Top Right:** The "0" with "5" above it is a bit ambiguous in its meaning; clarification or better visual integration could help.
            </Image-Analysis>
            <UI-Description>
1. Layout Overview
- Page structure: The application features a fixed-width left sidebar for primary navigation and a main content area on the right. The main content area uses a flexible grid system, arranging sections and content cards primarily in horizontal rows, with vertical stacking for smaller elements within cards. The overall application window appears to be a desktop-style application with standard window controls in the top-left.
- Component hierarchy: The top-level component is likely an `AppLayout` which contains a `Sidebar` and a `MainContentArea`. The `Sidebar` encapsulates `NavigationGroups` (for icon-only items), `NavigationCategories` (e.g., "书库", "我的藏书") which contain `NavigationItems`, and a `UserProfileWidget`. The `MainContentArea` is composed of multiple `Section` components, each with a `SectionHeader` and either a collection of `BookCard` components (e.g., `ContinueReadingCard`, `PreviouslyReadCard`) or a `ReadingGoalWidget`.
- Responsive design considerations: Optimized for desktop/wide screens. For smaller viewports, the `Sidebar` would likely collapse into a mobile navigation (e.g., hamburger menu), and `BookCard` components within sections would reflow to stack vertically instead of horizontally. Horizontal scrolling in sections like "之前读过" would become vertical scrolling or a paginated view.

2. Styling Specifications
- Color schemes:
    - Primary Background: `var(--color-white, #FFFFFF)` for `MainContentArea`.
    - Sidebar Background: `var(--color-light-grey-1, #F5F5F5)`.
    - Selected Navigation Item: `var(--color-lavender, #E8E7ED)`.
    - Text Colors:
        - Headings/Primary: `var(--color-dark-text, #333333)` or `var(--color-black, #000000)`.
        - Secondary/Labels: `var(--color-medium-grey-text, #666666)` or `var(--color-light-grey-text, #999999)`.
    - Icon Colors: `var(--color-dark-grey-icon, #666666)` or `var(--color-light-grey-icon, #BBBBBB)`.
    - Accent Colors: `var(--color-blue-green-accent, #BBE6F5)` (for badges/active states).
    - Book Card Backgrounds:
        - `ContinueReadingCard`: `var(--color-dark-green-grey, #4D5857)` and `var(--color-dark-red-grey, #4D3D40)`.
        - `PreviouslyReadCard`: `var(--color-white, #FFFFFF)` with subtle shadow.
- Typography:
    - Font Family: `var(--font-sans-serif, 'PingFang SC', 'Helvetica Neue', sans-serif)`.
    - Sizes:
        - `H1` (Page Title "主页"): `var(--font-size-xxl, 28px)` to `var(--font-size-xxxl, 32px)`.
        - `H2` (Section Headers): `var(--font-size-lg, 18px)` to `var(--font-size-xl, 22px)`.
        - `BookCard` Titles: `var(--font-size-md, 16px)` to `var(--font-size-lg, 18px)`.
        - `BookCard` Subtitles/Details: `var(--font-size-sm, 12px)` to `var(--font-size-base, 14px)`.
        - `Sidebar` Navigation: `var(--font-size-base, 14px)` to `var(--font-size-md, 16px)`.
        - `ReadingGoalWidget` Time: `var(--font-size-xxxxl, 40px)` to `var(--font-size-xxxxxl, 50px)`.
    - Weights: Headings `font-weight: var(--font-weight-bold, 700)`; Book Titles `font-weight: var(--font-weight-semibold, 600)`; Secondary text `font-weight: var(--font-weight-regular, 400)`.
- Spacing and alignment:
    - Vertical Spacing: Consistent `var(--spacing-lg, 24px)` between major sections; `var(--spacing-sm, 8px)` to `var(--spacing-md, 16px)` within components.
    - Horizontal Spacing: `var(--spacing-xl, 32px)` padding between `Sidebar` and `MainContentArea`; `var(--spacing-md, 16px)` to `var(--spacing-lg, 24px)` between `BookCard` items.
    - Alignment: Left-aligned text for headings, navigation, and card content.
    - Padding: `var(--padding-page-horizontal, 32px)` for `MainContentArea` content.
- Animations and transitions:
    - Expected `transition: all 0.2s ease-in-out` for hover states on interactive elements (navigation items, book cards).
    - Smooth `transition: background-color 0.2s ease-in-out` for active navigation items.
    - `ReadingGoalWidget` progress fill should animate smoothly.
    - Subtle `transform: translateY(-2px)` or `box-shadow` expansion on `BookCard` hover.

3. Interactive Elements
- User inputs: No explicit input fields are visible, but "新建藏书" implies a potential input for collection names.
- Navigation:
    - `SidebarNavigationItem` components are clickable, triggering route changes or content updates.
    - `SectionHeader` with arrow (e.g., "之前读过 >") is clickable to navigate to a dedicated view for that section.
    - `BookCard` components are clickable to view detailed book information.
    - Top-left sidebar icons are interactive for notifications, settings, etc.
- Loading states:
    - `SkeletonLoader` components for `BookCard` and `ReadingGoalWidget` while data is being fetched.
    - `Spinner` component for global loading or specific section loading.
- Micro-interactions:
    - Hover effects on `SidebarNavigationItem` (background color change, text/icon color change).
    - Subtle lift/shadow expansion on `BookCard` hover.
    - Visual feedback on click for all interactive elements.
    - `Badge` components on sidebar icons for unread counts or notifications.

4. Component Architecture
- Layout components:
    - `AppLayout`: The main container, orchestrating `Sidebar` and `MainContentArea`.
    - `Sidebar`: Fixed-width left panel for global navigation.
    - `MainContentArea`: Scrollable right panel for dynamic content.
    - `Section`: A generic container for content blocks within `MainContentArea`, including a `SectionHeader`.
    - `CardGrid`: A flexible grid layout component for arranging `BookCard` items.
- Feature components:
    - `SidebarNavigation`: Manages the state and rendering of navigation items.
    - `UserProfileWidget`: Displays user avatar and name in the sidebar.
    - `ContinueReadingSection`: Renders the "继续阅读" section with `ContinueReadingCard` components.
    - `PreviouslyReadSection`: Renders the "之前读过" section with `PreviouslyReadCard` components, potentially with horizontal scrolling.
    - `ReadingGoalWidget`: Displays the circular progress indicator and time.
- Shared components:
    - `SectionHeader`: Reusable component for section titles, potentially with an optional "view all" arrow.
    - `BookCard`: Abstract base component for book display, extended by `ContinueReadingCard` and `PreviouslyReadCard`.
    - `Icon`: A generic icon component, supporting various icon types (e.g., `HomeIcon`, `FolderIcon`, `CheckmarkIcon`, `PlusIcon`).
    - `Avatar`: Displays user profile images.
    - `Badge`: Displays numerical notifications or status indicators.
    - `ProgressBarCircular`: Reusable component for circular progress visualization.
    - `Scrollbar`: Custom styled scrollbar for consistent UI.
- Container components:
    - `HomePage`: The main container for the "主页" view, orchestrating the `ContinueReadingSection`, `PreviouslyReadSection`, and `ReadingGoalWidget`.
    - `BookListContainer`: A container that fetches and manages state for a list of books, passing data to `BookCard` components.
    - `NavigationContainer`: Manages the active state of sidebar navigation items.
      </UI-Description>